# ==================== Database Configuration ====================
# MongoDB connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/?appName=AppName
# Obtain connection string from MongoDB Atlas dashboard
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=AppName

# Database name in MongoDB cluster
DB_NAME=your_database_name

# ==================== JWT Authentication Configuration ====================
# Secret key for signing JSON Web Tokens
# Use a strong, random string in production (min 32 characters recommended)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-change-in-production

# ==================== Firebase Admin SDK Configuration ====================
# Base64-encoded Firebase Admin SDK JSON key file
# Get from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Encode the downloaded JSON file to base64: cat firebase-key.json | base64
FIREBASE_ADMIN_SDK_JSON=your-base64-encoded-firebase-admin-sdk-json

# ==================== Stripe Payment Configuration ====================
# Test/Live mode secret key from Stripe Dashboard > Developers > API Keys
# Never expose public keys in .env
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Public key for client-side Stripe integration
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
